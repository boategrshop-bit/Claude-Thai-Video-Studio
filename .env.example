# Copy ไฟล์นี้เป็น .env แล้วใส่ค่าจริง
# cp .env.example .env   (Mac/Linux)
# copy .env.example .env  (Windows)

# ElevenLabs Scribe — ใช้สำหรับ transcribe ภาษาไทย
# รับ key ได้ที่: https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=

# Optional: Anthropic key (ใช้เฉพาะถ้า run script นอก Claude Code)
# ANTHROPIC_API_KEY=
